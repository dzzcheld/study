package Quiz;

public class Source {
/*int[] intArray=new int[];(레퍼런스2byte
 * 실제 int가 아니다(할당되있지 않고 주소만 나타내주는거다)  =     초기화 역할,방을 만든다(공간확보)
 * intArray.length방의 수를세고 다시 가지고나옴
 * String str="abc"; 레퍼런스 클래스 2byte
 * String배열은 방에 값이 들어가지않는다(입장이 다르다)->>방(스트링을 받을수있는 레퍼런스 변수)에 있는애들은 2byte씩을 저장할수있다
 * 												null의 개념을 갖고 초기화를 한다.방안에서 주소값을 가리키는 형태로됨.
 * 											(int는 실직적인 값들을 방에 세겨넣고 온다-초기화할때0의 개녕임)
 * */
}
